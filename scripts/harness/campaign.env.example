# Copy to ~/.config/talens-harness/campaign.env. Read by the talens-research systemd service.
# PLAN is the ralphex-native phase plan to run (path relative to the repo root).
# Block A (consolidate) runs SUPERVISED/foreground via run_campaign.sh; point systemd at Block B
# (expansion) only AFTER Block A is complete and the site looks right.
PLAN=docs/plans/campaign-B-expand.md
