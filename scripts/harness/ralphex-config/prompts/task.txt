# talens harness — per-phase prompt DISPATCHER (recipe-keyed).
# ralphex feeds THIS file (with {{...}} variables substituted) to the executor every iteration.
# It is intentionally tiny: it reads the next phase's `recipe:` and routes to the shared preamble
# + the ONE matching recipe body, so the executor never loads the other three recipe bodies (the
# old monolith loaded all four every phase). The full monolith is kept at task.txt.monolith.bak.
# Recipe bodies live beside this file: _preamble.txt, consolidate.txt, full.txt, experiment.txt,
# theory.txt — ralphex loads only task.txt by name and ignores the rest. Plain text only.

You are the talens research-phase executor. Do EXACTLY this, in order:

STEP A — DETERMINE THE RECIPE OF THE NEXT PHASE
  Read the plan file {{PLAN_FILE}}. Find the FIRST `### Task` block whose `- [ ] run-phase:` line is
  still unchecked (`[ ]`, not `[x]`). Read that block's `recipe:` metadata line — it is exactly one
  of: consolidate | full | experiment | theory. Call this value RECIPE.
  If NO task is still unchecked, output exactly `<<<RALPHEX:ALL_TASKS_DONE>>>` and STOP (load nothing).

STEP B — LOAD YOUR INSTRUCTIONS
  Read these two files now and follow them together as ONE document, in this order:
    1. scripts/harness/ralphex-config/prompts/_preamble.txt          (the recipe-agnostic spine)
    2. scripts/harness/ralphex-config/prompts/<RECIPE>.txt           (your STEP 2 body; <RECIPE> = STEP A)
  Do NOT read or load the other recipe bodies. Execute the combined instructions exactly: the
  preamble's STEP 0/1, then the recipe body as STEP 2, then the preamble's STEP 3/4 and HARD RULES.

STEP C — RESOLVED VARIABLES
  The two files reference template tokens. Wherever they contain a {{TOKEN}}, substitute the
  already-resolved value from this table:
    PLAN_FILE      ->  {{PLAN_FILE}}
    PROGRESS_FILE  ->  {{PROGRESS_FILE}}
    GOAL           ->  {{GOAL}}
    DEFAULT_BRANCH ->  {{DEFAULT_BRANCH}}
    PLANS_DIR      ->  {{PLANS_DIR}}

Complete exactly ONE phase, then STOP. Output plain text only — no markdown decoration.
